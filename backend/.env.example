PORT=5050
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=campuspulse
DB_PORT=3306

# JWT Authentication
JWT_SECRET=campuspulse_super_secret_key_123!
JWT_EXPIRE=24h

# SMTP Email Configuration (Optional - for real email notifications)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@campuspulse.edu
